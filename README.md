# Drift Python SDK

## Installation

Manual method
```bash
python -m venv venv
. venv/bin/activate
pip install -r requirements.txt
```
Alternatively if you have [direnv](https://direnv.net/), just allow this folder,
and run `pip install -r requirements.txt`.

Then you can try the test script at `tests/test_flask_demo.py`.
